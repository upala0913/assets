#日志
logging.config = classpath:log4j2.xml

#服务设置
server.port = 8080

#设置视图
spring.mvc.view.prefix = /templates/
spring.mvc.view.suffix = .html

#jdbc连接
spring.datasource.driver-class-name = com.mysql.jdbc.Driver
spring.datasource.url = jdbc:mysql://localhost:3306/customer?useSSL=true&useUnicode=true&characterEncoding=UTF-8
spring.datasource.username = root
spring.datasource.password = wangpeng

#整合mybatis
mybatis.type-aliases-package = com.isoftstone.upala.assets.pojo
mybatis.mapper-locations = classpath:mapper/*.xml